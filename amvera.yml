meta:
  environment: python
  toolchain:
    name: pip
    version: 3.14
run:
  containerPort: 10000
  command: uvicorn main:app --host 0.0.0.0 --port 10000
build:
  buildCommand: pip install -r requirements.txt
